<h1>homepage for <%= @current_user.name %></h1>
  <div class="row">
    <div class="span6">
      <p>
        <% if @current_user.requested_friendships.any? %>
        <div class="alert alert-info alert-block">
          <a class="close" data-dismiss="alert" href="#">&times;</a>
          <h4 class="alert-heading">Pending Friendrequests</h4>
            You have friendrequests pending:
            <% for friend in @current_user.requested_friendships %>
            <li><%=  User.find(friend.user_id).name %></li>
            <% end %>
        </div>
        <% end %>
      </p>

      <ul id="myTab" class="nav nav-tabs">
            <li class="active"><a href="#profile" data-toggle="tab">Profile</a></li>
            <li class=""><a href="#golfround" data-toggle="tab">Golf Rounds</a></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Registrer round<b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><a href="#dropdown1" data-toggle="tab">usikker</a></li>
                <li><a href="#dropdown2" data-toggle="tab">@mdo</a></li>
              </ul>
            </li>
      </ul>

      <div id="myTabContent" class="tab-content">
            <div class="tab-pane fade active in" id="profile">
              <p>Her kan vi ha info om brukern</p>
            </div>
            <div class="tab-pane fade" id="golfround">
              <p>Statistikk på golf?</p>
            </div>
            <div class="tab-pane fade" id="dropdown1">
              <p>Kanskje kanskje ikke dropdown.</p>
            </div>
            <div class="tab-pane fade" id="dropdown2">
              <p>Evt flere da...:)</p>
            </div>
          </div>

    </div>
    <!-- span6 -->
    <div class="span3 offset3">
      <%= render 'shared/friend_listing' %> 
    </div>
    <!-- span4 -->
  </div>
  <!-- row -->
  